header.site-header-cell
  div
    = link_to root_path do
      = image_tag image_path("logos/atsb-w.svg")
  div
    = link_to 'Explora y califica', services_path, class: current_page?(:controller => 'home', :action => 'services') ? 'active' : ''
    = link_to 'El proyecto', about_path, class: current_page?(:controller => 'home', :action => 'about') ? 'active' : ''
    = link_to 'Te respondemos', 'https://medium.com/@atuserviciobogota', noopener: 'noopener', noreferrer: 'noreferrer', target: '_blank'
    = link_to 'Datos abiertos', datasets_path, class: current_page?(:controller => 'home', :action => 'datasets') ? 'active' : ''
    - if current_user
      = button_to 'Cerrar Sesión', sign_out_path, method: 'delete'
  div
    - if current_page?(:controller => 'home', :action => 'services')
      a.site-menu-button v-on:click="actionClicked"
        i :class="action"

javascript:
  $(function() {
    ATSB.Components['components/site-header']()
  });
