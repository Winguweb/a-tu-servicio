ATSB.Components['components/compare-provider-button'] = Backbone.View.extend({
  initialize: function(options) {
    _.bindAll(
      this,
      'links',
    )
    this.links()
  },
  links: function() {},
})
