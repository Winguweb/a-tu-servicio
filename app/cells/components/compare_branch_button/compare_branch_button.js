ATSB.Components['components/compare-branch-button'] = Backbone.View.extend({
  initialize: function(options) {
    _.bindAll(
      this,
      'links',
    )
    this.links()
  },
  links: function() {},
})
